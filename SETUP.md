# 설치 방법

## 1. Google 스프레드시트와 Apps Script 연결

1. 예산 DB로 사용할 Google 스프레드시트를 엽니다.
2. `확장 프로그램 → Apps Script`를 선택합니다.
3. 기본 코드를 모두 지운 뒤 이 폴더의 `Code.gs` 내용을 붙여넣고 저장합니다.
4. `배포 → 새 배포 → 웹 앱`을 선택합니다.
5. 실행 사용자는 `나`, 액세스 권한은 `모든 사용자`로 설정하고 배포합니다.
6. 발급된 `/exec` 주소를 `app.py`의 `API_URL`에 넣습니다.

현재 `app.py`에는 기존에 발급한 주소가 입력되어 있습니다. Apps Script 코드를
수정한 경우에는 `배포 → 배포 관리 → 수정 → 새 버전 → 배포`까지 진행해야
실제 웹 앱에 반영됩니다.

## 2. GitHub에 올릴 파일

- `app.py`
- `requirements.txt`

`Code.gs`와 `SETUP.md`도 보관용으로 함께 올려두면 나중에 수정하기 편합니다.

## 3. Streamlit Community Cloud 배포

1. GitHub 저장소의 기본 브랜치에 위 파일을 업로드합니다.
2. Streamlit Community Cloud에서 해당 저장소를 선택합니다.
3. Main file path는 `app.py`로 지정합니다.
4. 별도의 Secrets, 서비스 계정 JSON, Google API 키는 입력하지 않습니다.

정상 배포 후 데이터는 `예산내역` 시트에
`ID / 연월일 / 팀명 / 팀원 / 항목 / 금액` 순서로 저장됩니다.

팀명은 Streamlit 입력 화면에서 `기획팀 / 개발팀 / 제조팀 / 경영팀` 중
하나를 선택하도록 구성되어 있습니다.

팀원 구분은 `팀장 / 팀원`, 예산 항목은
`복리후생비 / 수선비 / 출장비 / 접대비` 중에서 선택합니다.

연월일은 앱의 최근 내역에서 `2026년 07월 29일` 형식으로 표시됩니다.
월별 요약표에서는 기존처럼 `2026년 07월` 단위로 합산됩니다.
